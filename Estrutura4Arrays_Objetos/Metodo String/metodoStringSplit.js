/**
 * o split divide uma string em uma array - transformação de dados 
 * cada elemnto sera determinado por um separador em comum
 * os mais utilizados sao: ponto e virgula,virgula,espaço
 */
const frase = "era uma vez no oeste";
const arrayDfrase = frase.split(" ")// estou dizendo que sera separado por (espaço em  branco)
console.log(arrayDfrase);// tela : 
 
